import { Student } from '../types';

// Static data is no longer used - results are fetched from Supabase database
export const studentsData: Student[] = [];

// Empty array - results are now fetched from the database
export const rankedStudents: Student[] = [];